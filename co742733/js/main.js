function f1(a, b) {
   
    return (a + b);  // The  function returns the product of p1 and p2
}

function arrays() {
    var pens;
    pens = ["red", "orange", "purple", "green", "merjanta"];
    console.log(pens);
}